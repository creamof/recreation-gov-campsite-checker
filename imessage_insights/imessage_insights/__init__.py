"""imessage_insights — a local, read-only explorer and group-thread analyzer for
macOS Messages (iMessage + SMS).

Everything except the LLM-backed `summarize`/`digest` commands runs entirely on
your Mac. No messages are ever sent from this tool.
"""

__version__ = "0.1.0"
